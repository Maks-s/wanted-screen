SAddons = SAddons or {}
SAddons.WantedScreen = SAddons.WantedScreen or {}

// Temps entre chaque avis de recherche
SAddons.WantedScreen.TimeToUpdate = 20

// Distance d'affichage de l'interface, 0 pour voir même de très loin
SAddons.WantedScreen.ViewDistance = 500

// Utilise un fondu au lieu de passer par un écran de chargement
SAddons.WantedScreen.DoFade = false

// Temps du fondu en secondes
SAddons.WantedScreen.FadeDuration = 1

// Affiche un texte défilant en bas de l'écran
SAddons.WantedScreen.DoScroll = false

// Texte à afficher en bas de l'écran
SAddons.WantedScreen.ScrollText = " Ce suspect est extremement dangereux. Si vous le voyez n'intervenez pas seul."

// Vitesse du texte en bas de l'écran
SAddons.WantedScreen.ScrollSpeed = 0.2